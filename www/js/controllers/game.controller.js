(function() {
    'use strict';

    angular
        .module('milbatBT')
        .controller('GameController', GameController);

    GameController.$inject = ['$scope'];

    /* @ngInject */
    function GameController($scope) {
        activate();

        function activate() {

        }
    }
})();
